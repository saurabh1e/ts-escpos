name: Release

on:
  push:
    tags:
      - 'v*'
      - 'latest'

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            build/bin/ts-escpos.exe
            build/bin/*-installer.exe
            build/bin/ts-escpos-lite.exe
            build/bin/ts-escpos-lite-32.exe
            build/bin/ts-escpos-lite-amd64-setup.exe
            build/bin/ts-escpos-lite-386-setup.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
